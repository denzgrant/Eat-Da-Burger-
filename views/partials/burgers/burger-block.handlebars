<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Time to Eat!{{else}}Devoured!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">Take Burger off the menu!</button>
</li>
